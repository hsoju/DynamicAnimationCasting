scriptname DynamicAnimationCasting

bool function ReplaceSpells(string asUUID, Spell[] akSpells) global native
{Replaces the spells of all dynamic events, which have the unique id, with the given spells.}